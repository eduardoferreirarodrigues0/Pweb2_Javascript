let verificar = parseInt(prompt("Inserir numero para verificar:"))

if (verificar > 0 ){
	alert("Número Positivo")

}else if (verificar < 0) {
	alert("Número Negativo")
}else{
	alert("Seu número é 0!")
}